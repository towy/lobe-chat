- name: Debug Secrets
  run: |
    echo "DOCKER_REGISTRY_USER is set: ${{ secrets.DOCKER_REGISTRY_USER != '' }}"
    echo "DOCKER_REGISTRY_PASSWORD is set: ${{ secrets.DOCKER_REGISTRY_PASSWORD != '' }}"
  shell: bash
